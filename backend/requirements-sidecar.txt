# Heavy-analytics SIDECAR deps — kept OUT of backend/requirements.txt so the main backend
# binary stays lean (stdlib-bias). These bundle into a SEPARATE per-platform PyInstaller binary
# (desktop/build-sidecar.sh + the Windows CI step). Pins are the versions validated to bundle +
# run from a frozen onefile binary (numba JIT works; ~70MB). numba/llvmlite/scipy are stumpy's
# transitive deps but pinned here for reproducible CI wheels.
#
# NOTE: mlxtend (Phase-4 association rules → drags sklearn/pandas/matplotlib) is intentionally
# NOT here yet — add it when Phase 4 lands so we don't bloat the binary before it's used.
numpy==2.5.3
stumpy==1.14.1
dtaidistance==2.5.1
numba==0.67.0
llvmlite==0.49.0
scipy==1.18.1
